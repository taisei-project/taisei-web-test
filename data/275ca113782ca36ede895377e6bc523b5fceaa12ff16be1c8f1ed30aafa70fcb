#version 300 es
precision mediump float;
precision highp int;

layout(location = 0) out highp vec4 fragColor;
in highp vec3 pos;

void main()
{
    fragColor = vec4((normalize(pos) * 0.5) + vec3(0.5), 1.0);
}

